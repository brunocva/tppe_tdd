import pytest

# Testes consolidados em `tests/test_partida.py`.
# Este arquivo é mantido apenas para compatibilidade histórica e está pulado pelo pytest.
pytest.skip("consolidado em tests/test_partida.py", allow_module_level=True)
